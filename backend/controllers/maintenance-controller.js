export const listMaintenance = async (req, res, next) => {
  res.json({ message: "listMaintenance placeholder" });
};

export const createMaintenance = async (req, res, next) => {
  res.json({ message: "createMaintenance placeholder" });
};

export const closeMaintenance = async (req, res, next) => {
  res.json({ message: "closeMaintenance placeholder" });
};
