export const fuelController = {
  listFuelLogs: async (req, res, next) => {
    res.json({ message: "listFuelLogs placeholder" });
  },
  createFuelLog: async (req, res, next) => {
    res.json({ message: "createFuelLog placeholder" });
  }
};

export const expenseController = {
  listExpenses: async (req, res, next) => {
    res.json({ message: "listExpenses placeholder" });
  },
  createExpense: async (req, res, next) => {
    res.json({ message: "createExpense placeholder" });
  }
};
